<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#d1b8e7"
    tools:context="yiwejeje.staticrecallapp.HomeScreenActivity">

    <!-- The primary full-screen view. This can be replaced with whatever view
         is needed to present your content, e.g. VideoView, SurfaceView,
         TextureView, etc. -->
    <TextView
        android:id="@+id/fullscreen_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_horizontal"
        android:keepScreenOn="true"
        android:text="@string/title_content"
        android:textColor="#7d669e"
        android:textSize="50sp"
        android:textStyle="bold" />

    <!-- This FrameLayout insets its children based on system windows using
         android:fitsSystemWindows. -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true">

        <LinearLayout
            android:id="@+id/fullscreen_content_controls"
            style="?metaButtonBarStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|center_horizontal"
            android:background="@color/black_overlay"
            android:orientation="horizontal"
            tools:ignore="UselessParent">

            <Button
                android:id="@+id/begin_button"
                style="?metaButtonBarButtonStyle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/begin_button"
                android:onClick="showItemsList"/>

        </LinearLayout>

        <ImageView
            android:id="@+id/magnifyingglass_icon"
            android:layout_width="188dp"
            android:layout_height="181dp"
            android:layout_gravity="left|center_vertical"
            android:src="@drawable/magnifying2"/>

<!--        <ImageView
            android:id="@+id/openbox_icon"
            android:layout_width="198dp"
            android:layout_height="182dp"
            android:layout_gravity="right|center_vertical"
            android:src="@drawable/box"/>-->

        <Button
            android:id="@+id/button1"
            android:layout_width="175dp"
            android:layout_height="144dp"
            android:layout_x="118dp"
            android:layout_y="95dp"
            android:background="@drawable/box"
            android:onClick="toast"
            android:text="Store "
            android:layout_gravity="right|center_vertical" />

    </FrameLayout>

</FrameLayout>
