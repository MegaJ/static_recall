<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="yiwejeje.staticrecallapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeScreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SearchViewActivity"
            android:parentActivityName=".HomeScreenActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable"/>
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="yiwejeje.staticrecallapp.HomeScreenActivity" />
        </activity>
        <activity
            android:name=".StoreLocationActivity"
            android:label="@string/title_activity_store_location"
            android:parentActivityName=".HomeScreenActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="yiwejeje.staticrecallapp.HomeScreenActivity" />
        </activity>
        <activity
            android:name=".SearchLocationScreen"
            android:label="@string/title_activity_search_location_screen"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value="yiwejeje.staticrecallapp.SearchViewActivity" />
    </application>

</manifest>
